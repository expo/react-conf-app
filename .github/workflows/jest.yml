name: Run Jest tests
on: [pull_request]

jobs:
   test:
      name: Jest
      runs-on: ubuntu-latest
      steps:
         - uses: actions/checkout@v4
         - name: 📦 Install dependencies
           run: yarn

         - name: 🎭 Run Jest
           run: yarn test
