name: Check ESLint
on: [pull_request]

jobs:
   test:
      name: ESLint
      runs-on: ubuntu-latest
      steps:
         - uses: actions/checkout@v4
         - name: 📦 Install packages
           run: yarn
         - name: 🎭 Run ESLint
           run: yarn lint